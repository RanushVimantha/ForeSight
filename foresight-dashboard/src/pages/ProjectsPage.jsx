import React from 'react';
import { Typography } from '@mui/material';

const ProjectsPage = () => {
    return (
        <div>
            <Typography variant="h4" gutterBottom>
                Projects
            </Typography>
            <Typography>Manage all projects and monitor AI model deployments here.</Typography>
        </div>
    );
};

export default ProjectsPage;
