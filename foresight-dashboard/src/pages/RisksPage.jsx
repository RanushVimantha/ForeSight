import React from 'react';
import { Typography } from '@mui/material';

const RisksPage = () => {
    return (
        <div>
            <Typography variant="h4" gutterBottom>
                Risks
            </Typography>
            <Typography>Monitor identified risks, incidents, and mitigations here.</Typography>
        </div>
    );
};

export default RisksPage;
